using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//drops power ups for player and checks if they already have a power up
//destroys power up object when grabbed
//makes lazer bigger
//makes swords track
public class PowerUp : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
